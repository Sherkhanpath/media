# MongoDB Atlas connection string.
# Get this from Atlas -> Database -> Connect -> "Drivers" -> copy the URI,
# then replace <password> with your actual database user password.
MONGODB_URI=mongodb+srv://khansher5498_db_user:o1sVjmAtzkEjJknG@cluster0.5rhydie.mongodb.net/?retryWrites=true&w=majority
# Optional: database name (defaults to "mediasequencer" if not set)
MONGODB_DB=mediasequencer

# Optional: port the server listens on (defaults to 8080)
PORT=8080
